name: build

on:
  workflow_dispatch:
  pull_request:
    branches: ["main"]

jobs:
  python-checks:
    uses: ./.github/workflows/tox.yml
    secrets: inherit
  cdk-checks:
    uses: ./.github/workflows/cdk.yml
    secrets: inherit
